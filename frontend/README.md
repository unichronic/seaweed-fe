# frontend

```bash
npm install
npm run dev
```

Uses `NEXT_PUBLIC_API_URL` and Firebase public env vars from `.env.local`.

For Vercel, set this folder as the project root and add the same env vars.
